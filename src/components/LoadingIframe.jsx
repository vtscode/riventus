import styled from "styled-components";
import LoadingGif from "../vendor/media/image/loading.gif";

const LoadingIframe = styled.div`
  background:url(${LoadingGif}) .5rem 3rem no-repeat;  
`;

export default LoadingIframe;